{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "merge",
  "description": "Merge windows (one per conflicted file, label merge-<hash>): core APIs including events, plus closing themselves. `destroy` is needed as well as `close` from Part 7 on: the result buffer lives in memory until every conflict is decided, so a close with work outstanding is intercepted, confirmed, and only then destroyed — a plain `close` would re-enter the same handler.",
  "windows": ["merge-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
