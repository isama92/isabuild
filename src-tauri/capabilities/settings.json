{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window (single instance, label `settings`): core APIs including events, plus closing itself on Escape or Ctrl/Cmd+W. No `destroy`, unlike the diff and merge windows: nothing here intercepts its own close, so there is no second pass to force through. No dialog permission either — the folder picker is driven from Rust, so no window can open a native dialog on its own.",
  "windows": ["settings"],
  "permissions": ["core:default", "core:window:allow-close"]
}
